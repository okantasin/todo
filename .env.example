# Postgres bağlantısı. Kendi kullanıcı/şifre/host'una göre düzenle.
# Brew (mac) kurulumunda kullanıcı = sistem kullanıcı adın, şifre yok.
DATABASE_URL="postgresql://USER@localhost:5432/lifeos?schema=public"
